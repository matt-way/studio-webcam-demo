export const run = state => {
  // This code is ran once on start, and then on every change
}

export const update = state => {
  // This code is ran once every iteration in render or work modes
}
